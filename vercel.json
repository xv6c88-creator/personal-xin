{
  "version": 2,
  "buildCommand": "node build.js",
  "outputDirectory": "dist",
  "builds": [
    { "src": "dist/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/dist/$1" }
  ]
}
